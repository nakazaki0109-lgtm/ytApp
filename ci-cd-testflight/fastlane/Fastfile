platform :ios, '13.0'

desc "Build the app"
lane :build do
  gym(scheme: "Release") # Replace with your app's scheme
end

desc "Upload to TestFlight"
lane :beta do
  build
  upload_to_testflight
end

desc "Deploy to TestFlight"
lane :deploy do
  beta
end

# You can add more lanes as needed for different tasks.